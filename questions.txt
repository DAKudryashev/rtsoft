1. может ли в одном деплое быть несколько девайсов? НЕТ, ТОЛЬКО ОДИН
2. может ли в одном девайсе быть несколько ресурсов (чтобы оба были задействованы при деплое)? ДА
3. важно обращать внимание только на наполнение девайсов, только на замапленные блоки? только на связи?
(Или на все сразу) ПРОВЕРИТЬ С МАПОМ
4. сколько может быть FBNetwork на одном ресурсе? ВСЕГДА ОДИН
5. что приоритетно (sys или fboot) FBOOT ПЕРВИЧЕН

- Инструмент похожий на diff, входные данные: sys файл, type library, fboot.
- Проверяем: наличие ФБ, блоки связаны одинаково, одинаковый их состав, состав входов с заранее заданными значениями.
- Разница выдается отсортированной по ФБ.
В проекте может быть больше одного вычислительного узла (девайсов). (имена узлов опционально в качестве входных значений)